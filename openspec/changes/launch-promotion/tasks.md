# Tasks

- [x] Confirm the GitHub repo `androidand/specsync` is public and set a repo description and topics (e.g. `openspec`, `github-issues`, `cli`, `go`) via the GitHub UI or `gh repo edit` — repo is public, description set, topics added (openspec, github-issues, cli, go)
- [ ] Decide on the unscoped npm name: `specsync` is currently unclaimed on the registry. Either publish the wrapper there too / as an alias, or standardize all promotion copy on `@androidand/specsync`.
- [x] Accept (or not) the commit-author email situation: history contains the owner's work and personal emails. Recommendation: accept as-is. — accepted as-is (3 emails: work, personal, github-actions[bot])
- [ ] Optional promotion follow-ups: tag/announce a new version with the latest features, and verify the Cloudflare Pages site deploy still works
