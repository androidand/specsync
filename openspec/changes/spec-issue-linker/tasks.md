# Tasks: Spec ↔ issue linker

- [ ] Define the `Linker` interface and a chained resolver
- [ ] Implement branch-name resolver with a configurable pattern
- [ ] Implement marker + cache resolvers (refactor existing Find/cache logic behind it)
- [ ] Support spec-first creation when no link resolves
- [ ] Support issue-first scaffolding: a new issue creates an intake change
