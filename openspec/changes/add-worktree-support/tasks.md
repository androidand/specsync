# Tasks

- [ ] Add `--worktree` flag to `specsync pull`
- [ ] Create or reuse worktree: `git worktree add ../worktrees/<repo>-<issue> <branch>`
- [ ] Checkout feature branch inside worktree
- [ ] Run pull/sync operation inside worktree
- [ ] `go build ./...`, `go test ./...`, `gofmt` clean
