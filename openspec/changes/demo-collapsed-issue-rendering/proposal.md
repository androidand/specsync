# Demo: collapsed issue rendering

## Why

This is a throwaway demo change, not real work — created to visually verify
that `tighten-synced-issue-bodies` (#153, merged) renders the way it was
designed to: title and task checklist visible by default, everything else
one click away.

## Proposed Changes

- Nothing. This proposal exists only to give the renderer non-trivial
  content in every optional section — original ask, design notes,
  discoveries, tasks — so the collapsed sections aren't empty when viewed
  on GitHub.

## Non-Goals

- **Not** a real change. Safe to close and delete once inspected.

## Release Notes

N/A — demo only.
