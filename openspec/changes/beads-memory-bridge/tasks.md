- [ ] Add Beads detection: `bd` on PATH or `.beads/` present; a single `beadsAvailable()` gate that every integration point checks (no-op when absent)
- [ ] Add a thin `beads.go` adapter wrapping `bd` CLI calls (`remember`, `dep add`); shell out only, no library dependency; std-lib only
- [ ] Memory record: build a compacted record (title, why, final scope, key decisions, issue URL) from an archived `Change`
- [ ] Hook the `archive` event (from `archive-retention-lifecycle`) to emit the memory record via `bd remember` when Beads is available
- [ ] Extend `specsync link` with `-type relates|supersedes|duplicates|blocks|parent`; store the type in `.specsync/links.json` / links.md
- [ ] Render link type into the issue `## Related` section; project the edge via `bd dep add` when Beads is available
- [ ] Dry-run: print the `bd` commands that would run; make zero `bd` calls
- [ ] Tests: detection gate off → identical behavior to today; memory record content; typed-link rendering; dry-run emits no `bd` calls
- [ ] Document the optional Beads bridge in the skill + README (clearly marked optional; activation conditions; what is sent)
- [ ] Build, install, smoke-test with and without `bd` on PATH
