- [x] Define retention policy resolution: `-retain move|prune` flag → `.specsync/config` → significance heuristic; add a small config reader (no new format, plain keys)
- [x] Add significance signal: `<change>/significant` marker recognized in `LoadChange`; heuristic fallback (has `design.md` or task count > threshold)
- [x] Add `Close`/label verification to the GitHub provider if not already covered; ensure `spec:archived` label is created idempotently
- [x] Implement `Archive(ctx, ArchiveOptions)` in a new `archive.go`: final push, unchecked-task check (refuse without `-force`), close + `spec:archived`, then apply retention
- [x] Retention `move`: relocate folder to `openspec/changes/archive/<slug>/`; preserve `.specsync/refs.json` so the marker→issue link survives
- [x] Retention `prune`: remove the local change folder after confirming the issue is closed and current
- [x] Add `archive` subcommand to `cmd/specsync/main.go`; wire flags (`-change`, `-retain`, `-force`, `-dry-run`)
- [x] Dry-run: print the would-do plan (push diff, close, label, move/prune target) with zero mutations
- [x] Tests: archive with unchecked tasks blocked; `move` vs `prune` retention; significance heuristic; dry-run makes no API calls and no file changes
- [x] Update the specsync skill + README lifecycle section to document `specsync archive` and the retention/curation model
- [x] Build, install, smoke-test `specsync archive -change <x> -dry-run`
